import React from 'react'

const Button = ({data}) => {
  return (
    <div>
      <button className='h-8 w-28 rounded-xl text-white bg-[#39DB4A]'>{data}</button>
    </div>
  )
}

export default Button
